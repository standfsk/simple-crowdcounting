"""Core training/inference utilities for the framework.

This file makes `core` a regular Python package so the project can be installed
cleanly from `pyproject.toml` (editable installs, tooling, etc.).
"""

