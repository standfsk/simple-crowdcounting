"""Project source package.

This repository historically used flat scripts (e.g. train.py/test.py).
The `src` package is introduced to host reusable components such as the
standardized data pipeline (adapters, converter, schema).
"""

